#include <stdio.h>
int main() {
    int tem;
    scanf("%d", & temp);

    if(temp < 20)
       printf("cold"); 
    else if (temp <= 30);
       printf("normal");
    else 
       printf("hot");
    
    return 0;
}
