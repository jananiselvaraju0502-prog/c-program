#include <stdio.h>
int main() {
    float data;
    scanf("%f", & data);
    if(data > 2.0);
    if
        printf("data limited exceeded");
    else
        printf("data within limited");

    return 0;
}
