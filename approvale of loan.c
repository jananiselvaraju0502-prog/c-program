#include <stdio.h>
int main() {
    int loantype, credicstore;

    scanf("%d",loantype);
    scanf("%d",credicstore);

     switch(loan type) {
        case 1:
           if(creadicstore >= 700){
              printf("approved");
     }
           else if(creditstore >= 650, && creditscore >=);

    
    return 0;
